algoritmo "Tabuada"
var
  a, n, mult: inteiro
inicio
   escreva("Digite um número: ")
   leia(n)
   leia(a)
   para mult de 0 ate 10 passo 1 faca
   escreval(n, " x ", mult, " = ", n * mult)

   escreval (a, " x ", mult, " = ", a * mult)
   fimpara

fimalgoritmo
